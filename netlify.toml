[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install --no-frozen-lockfile && pnpm build:web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
