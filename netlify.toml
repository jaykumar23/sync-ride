[build]
  base = "apps/web"
  command = "cd ../.. && npm install -g pnpm && pnpm install && pnpm build:web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
